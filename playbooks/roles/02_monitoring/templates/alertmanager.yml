route:
  group_by: ["alertname"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 1h
  receiver: webhook

receivers:
  - name: webhook
    webhook_configs:
      - url: "https://alert-relay.{{domain}}/{{ monitoring_alert_webhook_id }}"
        send_resolved: true
